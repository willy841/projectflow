# 2026-04-06

## projectflow / 正式資料閉環驗收目標已定

- 使用者最新指定的真正驗收情境，不是只看 DB schema / migration / DAL 是否存在，而是：
  - 三條主線前端可以新增正式資料
  - 不再吃假資料
  - 並順利接到最後文件產出
- 已將此驗收情境整理成正式工作主線文件：`MD26-projectflow-formal-data-closure-validation-plan-2026-04-06.md`

## projectflow / 後續預設工作排程已定

後續每次續接 `projectflow`，預設優先序改為：
1. 先完成 Phase B：plans + confirmations + snapshots
2. 再完成 Phase C：service layer
3. 再優先完成設計線正式資料閉環
4. 設計線驗通後，再做備品線
5. 最後才做 vendor 線與 package / 最終文件承接

## 邊界提醒

- 這條主線可視為之後續接 `projectflow` 時的高優先預設任務
- 但不代表可在沒有當下批准的情況下，自動開始新的實作動作
- 仍須遵守既有 post approval distribution / 明確批准後執行規則

## projectflow / 2026-04-06 本輪已完成的重要工程停點

- 已建立並推送 DB Phase 1 migration scaffold、smoke check 腳本、Phase 1 DAL / repositories / services。
- 已把設計線推到第一版正式資料閉環：
  - `/design-tasks` 列表頁可切 DB
  - 設計任務 detail / document 頁可切 DB
  - design plans API 已建立
  - confirm API 已建立
  - detail 頁已接真正 save / confirm 寫入路徑
  - document 頁已優先讀 latest confirmation snapshot
- 設計線相關推送停點包含：
  - `26f70f7` db-backed design task detail adapter
  - `cc42026` db-backed design task list and apis
  - `8127996` connect design detail actions to db apis
  - `a20fb19` read design documents from confirmation snapshots

## projectflow / 2026-04-06 部署驗收期間的重要判斷

- 使用者已明確批准：在達到驗收情境前，相關必要步驟可持續往下做，不需要每一步重新請示。
- 部署到 Vercel 後，build 問題曾依序修正：
  - design plan amount sanitizer regex
  - `@types/pg`
  - repository row generics 約束
  - row types 對齊 `QueryResultRow`
- 後續 runtime 驗收發現：
  - `/design-tasks` server runtime 會報 `TypeError: Invalid URL at Object.query`
  - 目前最優先問題已從前端流程切換為：Vercel 環境中的 PostgreSQL connection string 無法被 `pg` 接受
- 已補強 DB client 支援的 env 名稱：
  - `DATABASE_URL`
  - `POSTGRES_URL`
  - `POSTGRES_PRISMA_URL`
  - `POSTGRES_URL_NON_POOLING`
- 已推送 hotfix commit：`7c70ea4` (`fix: support common postgres env names`)

## projectflow / 2026-04-06 正式驗收卡點現況

- 使用者已在 Vercel 設好：
  - `DATABASE_URL`
  - `PROJECTFLOW_USE_DB_DESIGN=1`
- 但 runtime logs 顯示 DB URL 仍不是合法 PostgreSQL URL，導致 `/design-tasks` 不能進一步驗正式 DB 流程。
- 另外曾發現一個重要路由邊界：舊 mock 任務 id（例如 `spring-popup-2026-0`）不是 DB uuid，不能直接拿來驗正式 DB save / confirm；已加保護避免非 uuid 任務 route 直接打 DB API。
- 最新與使用者對齊的下一步不是再改前端流程，而是：先確認 Vercel 環境中的 `DATABASE_URL` 來源與格式（例如來自 Supabase / Neon / Railway 等哪家 provider），找出正確可用的 PostgreSQL connection string。

## projectflow / 2026-04-06 本輪驗收完成更新

- 設計線第一版正式資料閉環：已驗收完成
- 備品線第一版正式資料閉環：已驗收完成
- vendor 線目前只完成到 task / plans / confirm / snapshot / 第一版文件頁承接，尚未完成真正閉環

## projectflow / 2026-04-06 vendor 真閉環標準已提高

- 使用者明確要求：vendor 線只有在 `package / 最終文件承接層` 補齊後，才算真正閉環。
- 因此不能只以 vendor task 文件頁第一版承接視為完成。
- 後續 vendor 主線已改為：`vendor package / 最終文件承接完整功能模式`

## projectflow / vendor 後續完整功能方向（已定）

接下來 vendor 線應依此順序推進：
1. 建 db-backed vendor package adapter
2. 讓 `/vendor-packages` 改讀正式 package source
3. 讓 `/vendor-packages/[id]` 改讀正式 package source
4. package source 由 latest confirmation snapshot + project/vendor meta 派生
5. financial / summary 承接改接正式 package source
6. 完成 vendor 真閉環驗收
