## 2026-04-13 — Projectflow post-MD108 Phase P3 closure

- 完成 `Phase P3 — home overview active aggregation` 正式收口：
  - `MD130` P3 work package
  - `MD131` P3 progress
  - `MD132` P3 status
  - `MD133` P3 final closure
- P3 最終結論：
  - homepage 已從 mock dashboard 推進為 DB-backed active overview aggregation
  - 已完成 project count / in-progress / pending design / procurement / vendor / recent projects / active collected / outstanding
  - 已確認 recent projects DB truth 至少包含：`Projectflow DB 驗收主線樣本`、`Projectflow 驗收測試專案`、`正式測試0710`
  - 已確認 active aggregation DB truth：collected = `0`、outstanding = `602000`
- 本輪明確 scope guardrail：
  - 不混入 month aggregation
  - 不混入 Accounting Center extension
- 使用者明確指示：先做 P3 closure，`帳務中心先不要碰`
- 因此目前管理狀態應標記為：
  - `Phase P3 = CLOSED`
  - `Phase P4 / Accounting Center extension = DEFERRED`

## 2026-04-13 — Projectflow GitHub sync and DB schema check

- 已將 `project-mgmt` repo 內專案系統主線同步到 GitHub：
  - remote: `git@github.com:willy841/projectflow.git`
  - push: `main -> master`
  - 同步前相對 `origin/master` 為 ahead 89 / behind 0
  - 補了一個 repo 內程式 commit：`6ec39f5` (`chore: add personnel submit api logging`)
- 同步時刻意未把 workspace root 的私人/管理檔一起推上 GitHub：
  - `MEMORY.md`
  - `memory/*.md`
  - root 層 MD/handoff 檔
  - `.tmp/`、`test-results/` 等非 repo 內容
- 後續若要同步 projectflow 文件主線到 GitHub，需另做 repo/docs 級整理，不可直接把整個 workspace root 混推。
- 使用者要求盤點 DB 是否仍需補 Supabase SQL editor。
- 已整理 repo migration 與 DB 依賴後確認：
  - migration 清單至少包含 `20260405_projectflow_phase1.sql`、`20260406_financial_manual_costs.sql`、`20260407_projects_contact_event_fields.sql`、`20260411_financial_reconciliation_groups.sql`、`20260411_vendor_task_plans_add_vendor_name_text.sql`、`20260412_accounting_center_phase_a.sql`、`20260412_financial_quotation_read_model.sql`、`20260413_project_requirements.sql`、`20260413_vendor_data_batch1_foundation.sql`
- 已提供使用者一份 Supabase SQL editor 可直接貼上的 schema coverage 自查 SQL（檢查 tables / columns / indexes）。
- 依使用者回傳的完整截圖，最終收斂判斷為：
  - 這份 schema coverage check 在涵蓋範圍內為全綠
  - 目前沒有明確缺少 table / column / index 的結構型補件
  - 下一步應改查 trigger / function、bootstrap/backfill/data state，以及 live frontend ↔ DB validation，而不是再猜缺哪份 migration
- 中間曾因不完整截圖短暫誤判 `projects` 的 customer/activity 欄位缺失，後續已用完整截圖校正，最終結論以全綠為準。

## 2026-04-14 — Projectflow project detail / dispatch / UI cleanup closure

- 承接 `MD133` 之後，完成 `MD134-projectflow-post-md133-project-detail-dispatch-and-ui-closure-2026-04-14.md`，作為本輪正式 closure / handoff。
- 本輪完成主線：
  - project creation upstream existence
  - project requirements CRUD
  - execution items manual CRUD closure
  - Excel import persistence + bulk import first-round hardening
  - design / procurement / vendor dispatch closure
  - vendor matching 完整結構版（含 `design_tasks.vendor_id` / `procurement_tasks.vendor_id`）
  - project detail / dispatch UI cleanup
  - `/projects` active-only list cleanup
  - homepage overview cleanup
  - new project page cleanup
- 本輪重要 SQL editor 補件：
  - `design_tasks.vendor_id`
  - `procurement_tasks.vendor_id`
- 本輪最重要已修衝突：
  - execution items UI 與 DB truth 不一致
  - Excel import 只留在 client state
  - execution item edit/delete 只改 local state
  - dispatch 三線 vendor source 不一致
- 目前管理判斷：
  - 核心主線已可驗收
  - 系統完成度約 75–80%
- 建議下一輪主線：
  1. 測試資料治理
  2. Closeout / 已結案主線正式收斂
